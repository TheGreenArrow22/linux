# 03 – Files and Directories

## Viewing and creating files

```bash
cat file
cat > file
cat >> file
touch file.txt
````

## Directories

```bash
mkdir exampledir
cd exampledir
```

## Copy, move, remove

```bash
cp src dst
mv old new
rm file
rmdir emptydir
rm -r directory
```

`rm -r` does not ask questions. You should ask yourself first.
